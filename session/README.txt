This directory stores PHP session files for the application at runtime.

Security:
- Access is denied by the accompanying .htaccess (Apache 2.4+: "Require all denied").
- Keep this directory writable by the web server user/service.

